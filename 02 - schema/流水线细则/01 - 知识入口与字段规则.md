# 知识入口与字段规则

版本：v0.2.0

生命周期：Draft

日期：2026-06-22

适用范围：知识入口、全层 frontmatter 字段契约、字段枚举、YAML 序列化、knowledge_id、非 Markdown 材料包、来源说明、domain 扩展、GBrain DB 同步状态语义

## 1. 入口类型

| 入口 | 第一落点 | 说明 |
| --- | --- | --- |
| Obsidian 网页剪藏 | `00 - raw/00 - inbox` | 剪藏插件直接生成 Markdown。 |
| 用户手写 | `00 - raw/00 - inbox` | 用户手写、整理、导入材料先进入 inbox。 |
| Agent 回写 | GBrain DB raw -> `00 - raw/00 - inbox` | 先进入运行层即时记忆，再物化为本地 Markdown。 |
| 系统导入 | `00 - raw/00 - inbox` | 批量导入、迁移和外部系统导入先进入 inbox。 |

## 2. 入口约束

所有入口都必须进入 inbox 接收流程。

禁止绕过 inbox 直接进入领域 raw 目录。

禁止绕过 raw 直接生成 active wiki。

Agent 回写进入 GBrain DB raw 后，仍必须物化到本地 inbox，不能只存在于 DB。

非 Markdown 材料必须与同名 Markdown 说明卡组成材料包。

## 3. 全层 Frontmatter 契约

所有 raw、wiki、schema Markdown frontmatter 全量展示以下 14 个字段。

字段顺序固定：

```yaml
knowledge_id:
title:
knowledge_layer:
lifecycle_status:
source:
captured_at:
domain:
tags: []
wiki_page_type:
compile_status:
compiled_to: []
trust_level:
gbrain_db_sync_status:
gbrain_db_sync_error:
```

字段地图：

| 字段 | 含义 | 值类型 | 值生成位置 |
| --- | --- | --- | --- |
| `knowledge_id` | 稳定知识 ID。 | 固定格式字符串 | 入口、修复流程或 DB 返回。 |
| `title` | 知识标题。 | 字符串 | 入口或修复流程。 |
| `knowledge_layer` | 知识所属层级。 | 枚举 | 当前流水线阶段。 |
| `lifecycle_status` | 知识生命周期状态。 | 枚举 | 当前流水线阶段。 |
| `source` | 知识来源。 | 固定格式字符串 | 入口流程。 |
| `captured_at` | 知识进入系统的时间。 | 时间字符串 | 入口或修复流程。 |
| `domain` | 领域归属。 | 枚举 | inbox 路由或 schema 规则。 |
| `tags` | 正文主题标签。 | 字符串列表 | inbox 路由、wiki 编译或人工复核。 |
| `wiki_page_type` | wiki 页面形态。 | 枚举 | wiki 编译流程。 |
| `compile_status` | raw 编译状态。 | 枚举 | raw 编译流程。 |
| `compiled_to` | raw 被编译到哪些 wiki。 | 字符串列表 | raw 编译流程。 |
| `trust_level` | 可信等级。 | 枚举 | 当前流水线阶段。 |
| `gbrain_db_sync_status` | GBrain DB 同步状态。 | 枚举 | 同步流程。 |
| `gbrain_db_sync_error` | GBrain DB 同步失败原因。 | 字符串 | 同步流程。 |

字段地图外字段必须删除或迁移。

## 4. knowledge_id 生成规则

`knowledge_id` 是知识对象的稳定 ID，不是路径、标题或 Git commit。

格式：

```text
raw-<12位hash>
wiki-<12位hash>
schema-<12位hash>
```

生成来源：

| 场景 | 生成规则 |
| --- | --- |
| Agent 回写 raw | 优先使用 GBrain DB raw 返回的稳定 ID。 |
| 网页剪藏 raw | 由本地修复流程生成 `raw-<12位hash>`。 |
| 用户写入 raw | 由本地修复流程生成 `raw-<12位hash>`。 |
| 系统导入 raw | 有外部稳定 ID 时映射为 `raw-<12位hash>`；否则由本地修复流程生成。 |
| wiki 页面 | 页面首次创建时生成 `wiki-<12位hash>`，不随标题变化。 |
| schema 规则 | 规则文件首次创建时生成 `schema-<12位hash>`，不随文件名变化。 |

唯一性要求：

1. `knowledge_id` 一旦生成，不因文件移动、重命名、标题修改而改变。
2. 发现重复 ID 时，后创建者必须重新生成。
3. 说明卡和对应非 Markdown 原文件共享同一个 `knowledge_id`。
4. raw 编译成 wiki 后，raw 和 wiki 不共享同一个 `knowledge_id`；关系通过 `compiled_to` 和正文 `依据来源` 表达。
5. 无法安全生成时写 `unknown`，并阻断后续自动路由。

本地修复流程生成 hash 时，输入至少包含标准化后的相对路径、标题、捕获时间和正文指纹。

## 5. 枚举值

`knowledge_layer`：

```text
raw
wiki
schema
```

`lifecycle_status`：

```text
inbox
raw
candidate
active
archived
deprecated
rejected
```

`domain`：

```text
AI Work
Amazon
认知管理
财务投资
设计
产品
schema
unknown
not_applicable
```

`wiki_page_type`：

```text
索引页
对象页
主题页
项目页
流程页
决策页
政策页
模板页
案例页
对比页
not_applicable
```

`compile_status`：

```text
未编译
已编译
部分编译
跳过编译
暂缓编译
已过期
not_applicable
```

`trust_level`：

```text
raw
reviewed
canonical
deprecated
unknown
```

`gbrain_db_sync_status`：

```text
pending
synced
failed
excluded
not_applicable
```

## 6. Frontmatter YAML 序列化规则

字段顺序必须固定为第 3 章的 14 字段顺序。

字符串默认使用单引号。

单引号内部使用两个单引号转义。

数组统一使用 block list。

空数组写：

```yaml
tags: []
compiled_to: []
```

frontmatter 禁止多行字符串。

URL、冒号、井号、方括号、反斜杠必须作为字符串处理。

字段值未知时写：

```text
unknown
```

字段不适用时写：

```text
not_applicable
```

字段地图外字段必须删除或迁移，不保留。

合格示例：

```yaml
knowledge_id: 'raw-52f05a33a746'
title: 'Agent 资料提炼与 raw 回写 SOP'
knowledge_layer: 'raw'
lifecycle_status: 'inbox'
source: 'Agent回写：当前对话'
captured_at: '2026-06-21 20:43'
domain: 'unknown'
tags:
  - 'agent回写'
  - 'GBrain'
wiki_page_type: 'not_applicable'
compile_status: '未编译'
compiled_to: []
trust_level: 'raw'
gbrain_db_sync_status: 'synced'
gbrain_db_sync_error: 'not_applicable'
```

## 7. source 规则

`source` 固定格式：

```text
来源类型：来源引用
```

来源类型只允许：

```text
用户写入
Agent回写
网页剪藏
系统导入
unknown
not_applicable
```

示例：

```text
Agent回写：当前对话
网页剪藏：https://example.com/article
用户写入：Obsidian手写
系统导入：bulk-import-2026-06-22
unknown：unknown
not_applicable：not_applicable
```

`source` 用于人类快速识别来源，不承载完整机器追溯。

完整追溯写入正文 `来源说明` 或 `回写来源`。

## 8. 来源说明正文结构

除正式 wiki 页面已使用 `依据来源` 外，raw 和非 Markdown 说明卡正文必须包含 `来源说明`。

标准结构：

```markdown
## 来源说明

| 字段 | 值 |
| --- | --- |
| 来源类型 |  |
| 来源引用 |  |
| 捕获时间 |  |
| 原始作者 |  |
| 原始链接/会话/文件 |  |
```

网页剪藏必须记录 URL、网页标题、作者和剪藏时间。

用户手写至少记录：

```text
用户写入：Obsidian手写
```

Agent 回写按 `07 - Agent 回写闭环规则.md` 的 `回写来源` 结构执行。

非 Markdown 说明卡必须记录关联文件。

## 9. 非 Markdown 材料包规则

PDF、图片、截图、Office 文档等非 Markdown 原文件必须有同名 Markdown 说明卡。

原文件和说明卡共享同一个 `knowledge_id`。

说明卡正文必须包含：

```markdown
## 来源说明

## 关联文件

## 原始内容说明
```

`关联文件` 必须写明原文件名、相对路径、文件类型和文件数量。

移动、路由、归档时，说明卡和原文件必须作为材料包一起移动。

原文件缺失时，说明卡不得进入 wiki 编译，只能进入待处理。

说明卡缺失时，非 Markdown 原文件不得进入自动路由。

## 10. captured_at 规则

`captured_at` 固定格式：

```text
YYYY-MM-DD HH:mm
```

优先级：

1. 来源系统提供的捕获时间。
2. Markdown 已有合法时间。
3. 文件创建时间。
4. 文件修改时间。
5. 修复流程当前时间。

时间未知时写 `unknown`，并在正文 `来源说明` 写明原因。

## 11. tags 规则

`tags` 由当前流水线阶段生成。

inbox 阶段以 `02 - inbox 接收与路由分发规则.md` 为准。

所有 tag 必须是字符串。

单个 tag 不允许空格。

英文多词 tag 使用连字符。

## 12. compiled_to 规则

`compiled_to` 必须指向 wiki 页面路径、wiki 链接或稳定 ID。

raw 尚未编译时写：

```yaml
compiled_to: []
```

wiki 和 schema 层不适用时写：

```yaml
compiled_to: []
```

## 13. gbrain_db_sync_status 分层语义

`gbrain_db_sync_status` 的语义必须结合 `knowledge_layer` 解释。

| knowledge_layer | `synced` 含义 |
| --- | --- |
| raw | 已进入 GBrain DB raw 即时记忆或 raw 层同步，不代表正式知识。 |
| wiki | 已进入 GBrain DB 正式可检索知识。 |
| schema | 已进入 GBrain DB 规则知识，可作为规则依据。 |

其他状态含义：

| 状态 | 含义 |
| --- | --- |
| `pending` | 尚未同步或待同步。 |
| `failed` | 同步失败，原因写入 `gbrain_db_sync_error`。 |
| `excluded` | 按规则排除，不参与同步。 |
| `not_applicable` | 当前层级不适用。 |

Agent 回写 raw 的 `synced` 只表示即时 raw 记忆已写入，不表示 wiki 编译完成。

## 14. domain 扩展协议

新增 domain 必须同步以下位置：

1. `00 - raw` 顶层领域目录。
2. `01 - wiki` 主领域目录。
3. 本规则的 `domain` 枚举。
4. inbox dry-run 脚本的 `$AllowedDomain`。
5. raw / wiki / schema 模板。
6. 主手册活动规则清单或影响面映射。
7. 规则变更日志。

禁止只在某个文件里临时新增 domain。

新增 domain 前必须先确认该领域是否是长期知识域，而不是一次性项目、临时任务或 tag。

## 15. gbrain_db_sync_error 规则

`gbrain_db_sync_error` 无错误时写：

```text
not_applicable
```

同步失败时必须写清错误原因。

错误原因应包含：

1. 失败阶段。
2. 错误类型。
3. 可执行的下一步。

